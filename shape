<?php

require "./command/index.php";

use Command\ShapeHandler;

$msg = 'opa';
$missing = "\n some arguments is missing \n";

$command = new ShapeHandler;

if(isset($argv[1])){

    switch ($argv[1]) {
        case 'cross':
            $command->makeCross();
            break;
        case 'x':
            $command->makeX();
            break;
        
        default:
            # code...
            break;
    }

}else{
    echo $missing;
}
